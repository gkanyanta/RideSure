name: ridesure_passenger
description: RideSure Passenger - Motorcycle ride-hailing app for Zambia
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  socket_io_client: ^2.0.3+1
  geolocator: ^11.0.0
  google_maps_flutter: ^2.5.3
  url_launcher: ^6.2.3
  intl: ^0.19.0
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
